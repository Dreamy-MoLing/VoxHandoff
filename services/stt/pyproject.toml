[project]
name = "voxhandoff-stt"
version = "0.1.0"
description = "Versioned local streaming STT sidecar for VoxHandoff"
requires-python = ">=3.11,<3.14"
dependencies = ["faster-whisper==1.2.1"]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py311"

