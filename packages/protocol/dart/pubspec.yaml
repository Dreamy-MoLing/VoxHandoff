name: agent_talk_protocol
description: Generated VoxHandoff protocol bindings for Flutter clients.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  fixnum: ^1.1.1
  grpc: ^5.1.0
  protobuf: ^6.0.0
